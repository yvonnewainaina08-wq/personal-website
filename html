<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Yvonne Wanjiku Wainaina — Mediation &amp; International Arbitration</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:opsz,wght@8..60,400;8..60,500;8..60,600&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root{
    --paper: #F2EFE6;
    --ink: #1F2421;
    --ink-soft: #4A4F49;
    --sage: #6B7B5E;
    --wood: #A67C52;
    --line: #C9C2AF;
    --max: 660px;
  }
  *{box-sizing:border-box; margin:0; padding:0;}
  html{scroll-behavior:smooth;}
  body{
    background:var(--paper);
    color:var(--ink);
    font-family:'Inter', -apple-system, sans-serif;
    font-size:17px;
    line-height:1.6;
    -webkit-font-smoothing:antialiased;
  }
  a{color:var(--ink); text-decoration-color:var(--wood); text-underline-offset:3px;}
  a:hover{color:var(--sage);}
  .wrap{max-width:var(--max); margin:0 auto; padding:0 24px;}

  header.hero{padding:96px 0 64px;}
  .mark{width:64px; height:34px; margin-bottom:40px;}
  .mark line, .mark circle{stroke:var(--ink); stroke-width:1.4; fill:none;}
  .mark circle{fill:var(--sage);}
  h1{
    font-family:'Source Serif 4', Georgia, serif;
    font-weight:500;
    font-size:clamp(2.1rem, 5.6vw, 2.9rem);
    line-height:1.14;
    max-width:14ch;
  }
  .role{
    margin-top:18px;
    font-size:1.05rem;
    color:var(--ink-soft);
    max-width:42ch;
  }
  .role strong{color:var(--ink); font-weight:600;}

  nav{
    margin-top:40px;
    display:flex;
    flex-wrap:wrap;
    gap:24px;
    font-size:0.95rem;
  }
  nav a{color:var(--ink-soft);}

  section{padding:56px 0; border-top:1px solid var(--line);}
  h2{
    font-family:'Source Serif 4', Georgia, serif;
    font-weight:500;
    font-size:1.5rem;
    margin-bottom:20px;
  }
  section > p{max-width:62ch; color:var(--ink-soft);}
  section > p + p{margin-top:14px;}

  .focus-list{margin-top:8px;}
  .focus-item{
    display:flex;
    gap:20px;
    padding:18px 0;
    border-bottom:1px solid var(--line);
  }
  .focus-item:first-child{border-top:1px solid var(--line);}
  .focus-item .n{
    font-family:'Source Serif 4', serif;
    color:var(--wood);
    font-size:0.95rem;
    padding-top:2px;
    flex-shrink:0;
    width:2ch;
  }
  .focus-item .t{font-weight:600; margin-bottom:4px;}
  .focus-item .d{color:var(--ink-soft); font-size:0.96rem;}

  .exp-item{padding:20px 0; border-bottom:1px solid var(--line);}
  .exp-item:first-child{border-top:1px solid var(--line);}
  .exp-head{display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap;}
  .exp-role{font-weight:600;}
  .exp-org{color:var(--ink-soft); font-size:0.96rem; margin-top:2px;}
  .exp-date{color:var(--wood); font-size:0.88rem; white-space:nowrap; padding-top:2px;}
  .exp-desc{color:var(--ink-soft); font-size:0.96rem;
